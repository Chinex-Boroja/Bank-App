spring:
  #  config:
  #    activate:
  #      on-profile: prod
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/bankdb
    password: boroja24

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: 'true'

  mail:
    host: smtp.gmail.com
    port: 587
    username: ihediohachinedu21@gmail.com
    password: hifl zmhr wmdi xelf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true